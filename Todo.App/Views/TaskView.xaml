<UserControl x:Class="Todo.App.Views.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Todo.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
             DataContext="Task">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding Path=Name, Mode=TwoWay}"></TextBox>
            <TextBox Text="{Binding Path=Description, Mode=TwoWay}"></TextBox>
            <TextBox Text="{Binding Path=DueDate, Mode=OneWay}"></TextBox>
        </StackPanel>
    </Grid>
</UserControl>
